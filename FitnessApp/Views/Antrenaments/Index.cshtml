@model IEnumerable<FitnessApp.Models.Antrenament>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ID_Istoric)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Antrenament_Descriere)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID_Istoric)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Antrenament_Descriere)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Antrenament">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Antrenament">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Antrenament">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
