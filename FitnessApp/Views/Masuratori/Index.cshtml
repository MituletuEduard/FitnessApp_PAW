@model IEnumerable<FitnessApp.Models.Masuratori>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Greutate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Utilizator)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Greutate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Utilizator.ID_Utilizator)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Masuratori">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Masuratori">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Masuratori">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
